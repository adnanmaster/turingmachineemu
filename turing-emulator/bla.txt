src/
├── app/
│   ├── layout.tsx             # Global layout, fonts, providers
│   └── page.tsx               # Main view: Grid layout (Editor left, Canvas right)
├── components/
│   ├── ui/                    # shadcn/ui components (buttons, sliders, dialogs)
│   ├── editor/
│   │   └── CodeEditor.tsx     # Monaco or CodeMirror React wrapper
│   ├── turing-machine/
│   │   ├── Tape.tsx           # Framer Motion wrapper for the shifting tape
│   │   ├── TapeCell.tsx       # Individual cell rendering
│   │   └── MachineHead.tsx    # The static read/write head overlay
│   └── visualization/
│       └── StateGraph.tsx     # The Obsidian-style force-directed graph
├── lib/
│   ├── turing/
│   │   ├── compiler.ts        # Parses raw string code -> Rule objects
│   │   ├── engine.ts          # Core logic: reads rules, shifts tape, yields events
│   │   └── types.ts           # TypeScript interfaces (TapeState, Transition, etc.)
│   └── utils.ts               # shadcn utility functions (cn, tailwind-merge)
└── store/
    └── useTuringStore.ts      # Zustand store for global state management